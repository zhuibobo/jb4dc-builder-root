package com.jb4dc.builder.workflow.integrate;

import org.camunda.bpm.engine.task.Task;
import org.camunda.bpm.model.bpmn.instance.FlowNode;
import org.camunda.bpm.model.bpmn.instance.SequenceFlow;
import org.camunda.bpm.model.bpmn.instance.UserTask;

import java.util.List;
import java.util.Map;

public interface IWFTaskIntegratedService {

    //FlowNode getFlowNode(Task task);

    //List<SequenceFlow> getOutgoingSequenceFlow(String processDefinitionId, String taskDefinitionKey);

    //List<UserTask> getNextPossibleUseTask(Task task, Map<String, Object> vars);
}
